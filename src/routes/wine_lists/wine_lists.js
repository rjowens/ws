import React, { Component } from 'react';
import { observer } from 'mobx-react';
import DevTools from 'mobx-react-devtools';

class WineLists extends Component {
  render() {
    return (
      <div className="container">
        <input></input>
      </div>
    )
  }
}

export default WineLists;
